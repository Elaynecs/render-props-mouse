# Render Props com MouseTracker

Este projeto demonstra o uso do padrão **Render Props** em React, utilizando o rastreamento da posição do mouse na tela.

## 📌 Objetivo
Implementar um componente reutilizável que captura a posição do mouse e permite diferentes formas de renderização por meio de render props.

## 🧩 Estrutura principal
- `MouseTracker.jsx`: componente responsável por capturar a posição do mouse.
- `App.jsx`: utiliza o MouseTracker com diferentes formas de renderização.

## 🚀 Tecnologias
- React
- Vite
- JavaScript

## ▶️ Como executar o projeto
```bash
npm install
npm run dev
